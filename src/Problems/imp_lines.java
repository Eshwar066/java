/*
sorting 2D Array                                    //{inbuilt function used in leetcode}
        Arrays.sort(arr,(a,b)->Integer.compare(a[1],b[1]));


coverting long to int:
        int check=(int) sum;                        //(typecast)

Implementing set in hashset
         Set<String> set = new HashSet<>();          //(only unique elements are added into set)

Implementing Map in hashmap
          Map<Double, Integer> map=new HashMap<>();
*/
